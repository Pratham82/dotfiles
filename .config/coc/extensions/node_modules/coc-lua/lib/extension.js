"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.activate = void 0;
const tslib_1 = require("tslib");
const coc_nvim_1 = require("coc.nvim");
const tools_1 = require("./utils/tools");
const commands_1 = require("./commands");
const config_1 = require("./utils/config");
function activate(context) {
    return tslib_1.__awaiter(this, void 0, void 0, function* () {
        config_1.setStoragePath(context.storagePath);
        const config = coc_nvim_1.workspace.getConfiguration().get('lua', {});
        if (config.enable === false) {
            return;
        }
        const command = config.commandPath || (yield tools_1.luaLspBin());
        if (!(yield tools_1.commandExists(command))) {
            yield tools_1.installLuaLsp();
        }
        const serverOptions = { command };
        const clientOptions = {
            documentSelector: ['lua']
        };
        const client = new coc_nvim_1.LanguageClient('lua', 'lua-lsp', serverOptions, clientOptions);
        context.subscriptions.push(coc_nvim_1.services.registLanguageClient(client), coc_nvim_1.commands.registerCommand("lua.version", () => commands_1.version()), coc_nvim_1.commands.registerCommand("lua.update.lua-lsp", () => commands_1.updateLuaLsp(client)));
    });
}
exports.activate = activate;
//# sourceMappingURL=extension.js.map