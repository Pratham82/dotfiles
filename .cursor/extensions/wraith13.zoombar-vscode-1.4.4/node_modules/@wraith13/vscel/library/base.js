"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.simplyDeepCopy = void 0;
exports.simplyDeepCopy = (object) => {
    switch (typeof object) {
        case "object":
            if (Array.isArray(object)) {
                const result = [];
                object.forEach((value, index) => result[index] = exports.simplyDeepCopy(value));
                return result;
            }
            else {
                const result = {};
                Object.keys(object).forEach(i => result[i] = exports.simplyDeepCopy(object[i]));
                return result;
            }
        case "function":
            // 関数の場合だけそのまま返す。
            return object;
        default:
            return JSON.parse(JSON.stringify(object));
    }
};
//# sourceMappingURL=base.js.map