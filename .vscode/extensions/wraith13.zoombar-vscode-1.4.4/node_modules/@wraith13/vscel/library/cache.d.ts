export declare class Cache<keyT, valueT> {
    loader: (key: keyT) => valueT;
    cache: {
        [key: string]: valueT;
    };
    constructor(loader: (key: keyT) => valueT);
    get: (key: keyT) => valueT;
    private getCore;
    getCache: (key: keyT) => valueT;
    clear: () => {};
}
